{
  "mcpServers": {
    "campus_virtual_pucp": {
      "command": "${PUCP_MCP_LAUNCHER}",
      "args": ["campus_virtual_pucp", "--client", "claude-desktop"]
    },
    "paideia": {
      "command": "${PUCP_MCP_LAUNCHER}",
      "args": ["paideia", "--client", "claude-desktop"]
    },
    "pucp_academic_overview": {
      "command": "${PUCP_MCP_LAUNCHER}",
      "args": ["pucp_academic_overview", "--client", "claude-desktop"]
    }
  }
}
