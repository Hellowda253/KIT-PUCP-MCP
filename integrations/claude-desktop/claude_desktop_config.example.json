{
  "mcpServers": {
    "campus_virtual_pucp": {
      "command": "${NODE_EXECUTABLE}",
      "args": [
        "${PUCP_MCP_ROOT}/servers/campus-virtual-pucp/src/server.js"
      ]
    },
    "paideia": {
      "command": "${NODE_EXECUTABLE}",
      "args": [
        "${PUCP_MCP_ROOT}/servers/paideia/src/server.js"
      ]
    },
    "pucp_academic_overview": {
      "command": "${NODE_EXECUTABLE}",
      "args": [
        "${PUCP_MCP_ROOT}/servers/overview/src/server.js"
      ]
    }
  }
}
