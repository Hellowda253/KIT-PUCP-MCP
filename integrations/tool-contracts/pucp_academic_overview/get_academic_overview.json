{
  "name": "get_academic_overview",
  "description": "Combine cached Paideia and Campus Virtual schedule, deadlines, grades, materials, and status-only alerts.",
  "parameters": {
    "type": "object",
    "properties": {
      "start": {
        "type": "string",
        "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
      },
      "end": {
        "type": "string",
        "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
      },
      "days": {
        "type": "integer",
        "minimum": 1,
        "maximum": 90
      },
      "limit": {
        "type": "integer",
        "minimum": 1,
        "maximum": 200
      }
    },
    "required": [],
    "additionalProperties": false
  }
}
