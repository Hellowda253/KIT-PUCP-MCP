{
  "mcpServers": {
    "campus_virtual_pucp": {
      "command": "${PUCP_MCP_LAUNCHER}",
      "args": ["campus_virtual_pucp", "--client", "claude-code"]
    },
    "paideia": {
      "command": "${PUCP_MCP_LAUNCHER}",
      "args": ["paideia", "--client", "claude-code"]
    },
    "pucp_academic_overview": {
      "command": "${PUCP_MCP_LAUNCHER}",
      "args": ["pucp_academic_overview", "--client", "claude-code"]
    }
  }
}
