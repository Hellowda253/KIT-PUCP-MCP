{
  "mcpServers": {
    "campus_virtual_pucp": {
      "command": "${PUCP_MCP_LAUNCHER}",
      "args": ["campus_virtual_pucp", "--client", "cursor"]
    },
    "paideia": {
      "command": "${PUCP_MCP_LAUNCHER}",
      "args": ["paideia", "--client", "cursor"]
    },
    "pucp_academic_overview": {
      "command": "${PUCP_MCP_LAUNCHER}",
      "args": ["pucp_academic_overview", "--client", "cursor"]
    }
  }
}
